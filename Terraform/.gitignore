# ==========================================
# Terraform / OpenTofu Professional .gitignore
# ==========================================

# 1. Local .terraform directories
# Contains providers, modules, and transient data.
**/.terraform/*

# 2. State files
# These contain sensitive infrastructure data and should NEVER be in Git.
# Use a remote backend (S3/GCS/Azure) instead.
*.tfstate
*.tfstate.*

# 3. Crash log files
crash.log
crash.*.log

# 4. Variables and Secrets
# Exclude all .tfvars files as they likely contain sensitive data.
*.tfvars
*.tfvars.json

# 5. Override files
# Used for local adjustments; should not be committed.
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# 6. Plan files
# Binary plan files can be large and are environment-specific.
*.tfplan

# 7. Lock files
# NOTE: .terraform.lock.hcl is usually COMMITTED to version control to 
# ensure consistent provider versions across environments.
# Uncomment the line below ONLY if you explicitly want to ignore it.
# .terraform.lock.hcl

# 8. Transient lock info files
.terraform.tfstate.lock.info

# 9. CLI configuration files
.terraformrc
terraform.rc

# 10. OS/Editor Junk
.DS_Store
Thumbs.db
*.swp
*.bak
.idea/
.vscode/